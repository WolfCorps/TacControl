<Window x:Class="TacControl.Dialogs.DialogOptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TacControl.Dialogs"
        mc:Ignorable="d"
        Title="TacControl - Settings" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <TabControl >
            <TabItem Header="Map Options">



                <StackPanel>
                    <CheckBox Content="Make markers that aren't in currently selected channel slightly transparent" IsChecked="{Binding MapSettings.TransparentOffchannelMarkers, Mode=TwoWay}"/>

                    <Button Content="test" Click="ButtonBase_OnClick"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
